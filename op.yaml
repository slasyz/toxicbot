actions:
  copy_files:
    - src: ./
      dest: ./
      exclude: [ .git, venv ]
    - ./config.json
  before_reloading:
    - 'make deps'

systemd:
  - ./systemd/toxic-main.service
  - ./systemd/toxic-server.service
