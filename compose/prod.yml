# DO NOT EDIT.  The code was generated by https://github.com/slasyz/op util.
version: '3.9'

services:
  app:
    volumes:
      - type: bind
        source: /etc/toxic/config.json
        target: /etc/toxic/config.json
        read_only: true
    ports:
      - 127.0.0.1:30120:8000/tcp
      - 127.0.0.1:30122:8001/tcp
  db:
    volumes:
      - type: bind
        source: /etc/toxic/config.json
        target: /etc/toxic/config.json
        read_only: true
    ports:
      - 127.0.0.1:30121:5432/tcp
